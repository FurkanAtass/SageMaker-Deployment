[project]
name = "sagemaker-deployment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "mlflow>=3.12.0",
    "ultralytics>=8.4.47",
]
